# Simple-Web-Scraper
This Python project is a simple web scraper that extracts data from a user-provided website and saves it into a CSV file. It demonstrates the basics of web scraping using Python, including sending HTTP requests, parsing HTML content with BeautifulSoup, and storing structured data in CSV format.
